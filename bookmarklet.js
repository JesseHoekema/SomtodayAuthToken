javascript:(function(){    try {        var tokens = [];        for(var i=0;i<localStorage.length;i++){            var key = localStorage.key(i);            if(key && key.indexOf('CapacitorStorage.') === 0){                try{                    var parsed = JSON.parse(localStorage.getItem(key));                    if(parsed && parsed.access_token) tokens.push(parsed.access_token);                }catch(e){}            }        }        if(tokens.length === 0){            alert('No access_token found!');            return;        }        var token = tokens[0];        var targetURL = 'https://projects.jessehoekema.com/SomtodayAuthToken?access_token=%27 + encodeURIComponent(token);        window.location.href = targetURL;    } catch(e){        alert(%27Error: %27 + e);    }})();
